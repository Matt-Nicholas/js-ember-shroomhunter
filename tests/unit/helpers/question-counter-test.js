
import { questionCounter } from 'js-ember-messageboard/helpers/question-counter';
import { module, test } from 'qunit';

module('Unit | Helper | question counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionCounter([42]);
  assert.ok(result);
});

